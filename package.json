{
  "name": "taaks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "typecheck:react": "npm run -w taaks-react typecheck",
    "typecheck": "run-s typecheck:*",
    "format:react": "npm run -w taaks-react format",
    "format": "run-s format:*",
    "format-check:react": "npm run -w taaks-react format-check",
    "format-check": "run-s format-check:*",
    "lint:react": "npm run -w taaks-react lint",
    "lint": "run-s lint:*",
    "lint-check:react": "npm run -w taaks-react lint-check",
    "lint-check": "run-s lint-check:*",
    "build:react": "npm run -w taaks-react build",
    "build": "run-s build:*",
    "generate:spring-boot": "cd backend && ./gradlew generateOpenApiDocs",
    "generate:react": "npm run -w taaks-react generate",
    "generate": "run-s generate:*",
    "dev:react": "npm run -w taaks-react dev",
    "dev:spring-boot": "cd backend && ./gradlew bootRun",
    "dev": "run-p dev:*",
    "storybook": "npm run -w taaks-react storybook",
    "storybook-build": "npm run -w taaks-react storybook-build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LY-HackU-2025-team8/taaks.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/LY-HackU-2025-team8/taaks/issues"
  },
  "homepage": "https://github.com/LY-HackU-2025-team8/taaks#readme",
  "workspaces": [
    "frontend_react"
  ],
  "devDependencies": {
    "npm-run-all2": "^8.0.1"
  }
}
